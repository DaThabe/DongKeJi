<UserControl
    x:Class="DongKeJi.Work.UI.View.Staff.UserStaffSelectorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:staffVm="clr-namespace:DongKeJi.PerformanceRecord.ViewModel.Staff"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:staff="clr-namespace:DongKeJi.Work.ViewModel.Staff"
    d:DataContext="{d:DesignInstance staffVm:UserStaffServiceViewModel}"
    mc:Ignorable="d">

    <ComboBox ItemsSource="{Binding Items}" SelectedItem="{Binding Selected}">

        <ComboBox.ItemTemplate>
            <DataTemplate DataType="{x:Type staff:UserStaffViewModel}">

                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <!--<ui:SymbolIcon Symbol="{Binding Avatar}" />-->
                    <ui:TextBlock Text="{Binding User.Name}" />
                </StackPanel>

            </DataTemplate>
        </ComboBox.ItemTemplate>
    </ComboBox>

</UserControl>