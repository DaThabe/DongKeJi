<UserControl
    x:Class="DongKeJi.Work.UI.View.Staff.StaffSelector"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:DongKeJi.Work.ViewModel.Staff"
    d:DataContext="{d:DesignInstance vm:StaffSelectorViewModel,
                                     IsDesignTimeCreatable=False}"
    mc:Ignorable="d">

    <ComboBox
        ItemsSource="{Binding ItemsSource}"
        SelectedItem="{Binding Selected}"
        ToolTip="员工选择">

        <ComboBox.ItemTemplate>
            <DataTemplate DataType="{x:Type vm:StaffViewModel}">

                <ui:TextBlock Text="{Binding Name}" />

            </DataTemplate>
        </ComboBox.ItemTemplate>
    </ComboBox>

</UserControl>